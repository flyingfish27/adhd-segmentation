# Python 虚拟环境
.venv/
venv/
__pycache__/
*.pyc

# Jupyter
.ipynb_checkpoints/

# macOS
.DS_Store

# 数据文件(按你的实际数据目录/后缀调整)
data/
*.csv
# 例外:analysis/labels/ 下的是【版本化配置表】(常模数值、文献出处),不是数据产物。
# 标签规则层靠它们复现,必须入库;否则 clone 下来跑 43_target_labels.py 会缺表。
!analysis/labels/*.csv
*.parquet
*.numbers
literature/
.venv/ # .venv 本身别提交，几百 MB
